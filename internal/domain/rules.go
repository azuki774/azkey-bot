// Package domain contains the small set of business values shared by the
// application layers.
package domain

// Rules represents the currently supported rules configuration.
//
// The foundation accepts only an empty rules list. Rule fields and runtime
// state will be introduced with the business requirements that define them.
type Rules struct{}
