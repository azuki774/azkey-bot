# このファイルを .env にコピーして実際の値を設定してください

# Misskey アクセストークン（必須）
# Misskey の設定 -> API -> アクセストークンの生成 から取得
# 必要な権限: タイムライン読み取り、ユーザー情報読み取り、フォロー実行、リアクション追加
i=your_misskey_access_token_here

# OpenRouter API キー（必須）
# https://openrouter.ai/keys から取得
OPENROUTER_API_KEY=your_openrouter_api_key_here

# オプション: CSV データディレクトリ（デフォルト: カレントディレクトリ）
# roumu.csv ファイルを保存する場所を指定
# Docker での例: /app/data
# ROUMU_DATA_DIR=/path/to/data/directory

# Docker Compose 用の環境変数（docker-compose.yml で使用）
MISSKEY_TOKEN=your_misskey_access_token_here
